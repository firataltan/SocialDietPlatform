import { createTheme } from '@mui/material';

export const theme = createTheme({
  palette: {
    primary: {
      main: '#2e7d32', // Yeşil tonu
    },
    secondary: {
      main: '#ff9800', // Turuncu tonu
    },
  },
  typography: {
    fontFamily: '"Roboto", "Helvetica", "Arial", sans-serif',
  },
}); 